scripts:
  - ./scripts/init.sh

commands:
  - name: shell
    path: commands/shell.sh
    description: Start a new baseops shell

config:
  name: "{{ .Env.CLI_NAME }}"
  description: "{{ .Env.CLI_DESCRIPTION }}"
  log:
    level: info
    prefix: "[{{ .Env.CLI_NAME}}] "
  helpMode: default
